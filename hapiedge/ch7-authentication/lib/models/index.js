//go through all files in given directory and require them
var load = function (options, callback) {

    options = options || {};
    options.extension = options.extension || '.js';
    var models = {};
    
}